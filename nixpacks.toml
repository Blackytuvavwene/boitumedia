[phases.setup]
nixPkgs = ["nodejs_24", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm prune --omit=dev"]

[start]
cmd = "node build"

[variables]
NODE_ENV = "production"
PORT = "3000"
HOST = "0.0.0.0"
